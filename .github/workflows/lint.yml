name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # DISABLED: Hourly schedule run - per user request 2026-04-16
  # schedule:
  #   # Run every hour at minute 0
  #   - cron: '0 * * * *'

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run TypeScript check
        run: npx tsc --noEmit
